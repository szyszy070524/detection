{
  "name": "skills-risk-scanner",
  "private": true,
  "version": "0.1.0",
  "description": "Static Vite app for locally scanning skill ZIP packages in the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
